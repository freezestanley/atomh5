/**
 * 通用Table组件, 还没想好怎么封装
 */
import React, { memo } from 'react'
import { useAntdTable } from 'ahooks'
interface PropTypes {}
function CommonTable(props: PropTypes) {
  return <div></div>
}
export default memo(CommonTable)
